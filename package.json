{
  "name": "serverless-python-demo",
  "version": "1.0.0",
  "scripts": {
    "localstack:start": "docker run -d --rm --name localstack -p 4566:4566 localstack/localstack",
    "localstack:stop": "docker stop localstack",
    "dev": "serverless deploy --stage offline"
  },
  "devDependencies": {
    "serverless": "^4.17.2",
    "serverless-localstack": "^1.3.1"
  }
}
