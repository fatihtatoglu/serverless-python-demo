{
  "name": "serverless-python-demo",
  "version": "1.0.0",
  "scripts": {
    "dev:prepare": "docker compose up -d",
    "dev:start": "serverless deploy --stage offline",
    "dev:stop": "serverless remove --stage offline",
    "dev:info": "serverless info --stage offline",
    "dev:clean": "docker compose down"
  },
  "devDependencies": {
    "serverless": "^3.39.0",
    "serverless-localstack": "^1.3.1"
  }
}